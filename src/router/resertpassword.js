import ResetPassword from '../views/auth/ResetPassword.vue';
const routes = [
    {
        path: '/reset/update-password',
        name: 'reset/update-password',
        component: ResetPassword
    }
]

export default routes