import ResetPassword from '../views/auth/ResetPassword.vue';
const routes = [
    {
        path: '/reset-password',
        name: 're-password',
        component: ResetPassword
    }
]

export default routes